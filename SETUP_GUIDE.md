# 從零開始設定指南（ESP32 版）

> 本指南適用於從零開始組裝本專題無人機，涵蓋從 ESP32 開發板開箱到實機飛行的完整流程：軟體安裝、感測器接線（VL53L1X 測距 + PMW3901 光流）、ESP32 內建藍牙通訊、Betaflight 飛控設定、電調馬達焊接、ESP32 供電與 brownout 排查，以及速度模式定高的首飛操作。
>
> 飛控韌體使用 **Betaflight**（透過 IBUS 接收 RC 訊號）。定高與光流補正由 **ESP32 自行運算**，不依賴飛控的定高/定點功能。

---

## 系統架構

```
[PS 手把] --藍牙--> [電腦 main_esp32_velmode.py] --藍牙(SPP)--> [ESP32] --IBUS--> [Betaflight 飛控 F405] --> 四軸馬達
                                                  <--高度/光流/PID回傳--      |
                                                                            +--PWM--> [夾爪伺服馬達 GPIO13]
                                                                            +--I2C--> [VL53L1X 測距感測器]
                                                                            +--SPI--> [PMW3901 光流感測器]
```

| 元件 | 角色 |
|------|------|
| PS 手把 | 透過藍牙與電腦配對，作為輸入裝置（無手把可用鍵盤）|
| 電腦（`main_esp32_velmode.py`）| 讀手把輸入 → 打包 10-byte 封包 → ESP32 內建藍牙；接收高度/光流/PID 遙測並即時繪圖 |
| ESP32（`ino_for_esp32/ino_for_esp32.ino`）| 收指令 → 轉 IBUS 給飛控；讀 VL53L1X 跑速度 PID 定高；讀 PMW3901 光流；控制夾爪；藍牙回傳遙測 |
| VL53L1X | I2C 雷射測距，提供高度給 ESP32 定高 PID |
| PMW3901 | SPI 光流感測，提供水平位移給水平漂移補正/記錄 |
| Betaflight 飛控 F405 | 收 IBUS 訊號，以自穩（ANGLE）模式驅動四軸馬達 |

---

## 目錄

**Part 1：ESP32、感測器與藍牙（地面站）**

1. [所需材料](#1-所需材料)
2. [安裝 Arduino IDE 與 ESP32 開發板支援](#2-安裝-arduino-ide-與-esp32-開發板支援)
3. [驗證 ESP32 是否正常（Blink）](#3-驗證-esp32-是否正常blink)
4. [安裝感測器與伺服函式庫](#4-安裝感測器與伺服函式庫)
5. [ESP32 接腳總表](#5-esp32-接腳總表)
6. [VL53L1X 測距感測器接線](#6-vl53l1x-測距感測器接線)
7. [PMW3901 光流感測器接線](#7-pmw3901-光流感測器接線)
8. [夾爪伺服馬達接線](#8-夾爪伺服馬達接線)
9. [燒錄韌體到 ESP32](#9-燒錄韌體到-esp32)
10. [電腦配對 ESP32 藍牙](#10-電腦配對-esp32-藍牙)
11. [安裝 Python 環境](#11-安裝-python-環境)
12. [用 test_esp32_bt.py 測試藍牙通訊](#12-用-test_esp32_btpy-測試藍牙通訊)
13. [測試手把連接](#13-測試手把連接)
14. [執行主程式](#14-執行主程式)
15. [（備用）鍵盤控制模式](#15-備用鍵盤控制模式)

**Part 2：ESP32 與 Betaflight 飛控連接**

16. [連接前注意事項](#16-連接前注意事項)
17. [飛控板連接電腦與 Betaflight 韌體](#17-飛控板連接電腦與-betaflight-韌體)
18. [設定 IBUS 通訊協議](#18-設定-ibus-通訊協議)
19. [陀螺儀校正](#19-陀螺儀校正)
20. [ESP32 與飛控板接線](#20-esp32-與飛控板接線)
21. [Betaflight 安全設定](#21-betaflight-安全設定)
22. [通道對應與訊號確認](#22-通道對應與訊號確認)
23. [模式綁定](#23-模式綁定)
24. [解鎖失敗排查](#24-解鎖失敗排查)

**Part 3：電調、馬達與電源**

25. [焊接安全警告](#25-焊接安全警告)
26. [電調與電源線焊接](#26-電調與電源線焊接)
27. [電調與馬達焊接](#27-電調與馬達焊接)
28. [上電安全檢查](#28-上電安全檢查)
29. [用 Betaflight 確認馬達連接](#29-用-betaflight-確認馬達連接)
30. [手把控制馬達整合測試](#30-手把控制馬達整合測試)
31. [⭐ ESP32 供電與 Brownout 斷線排查](#31--esp32-供電與-brownout-斷線排查)

**Part 4：實機飛行與速度模式定高**

32. [飛行前安全清單](#32-飛行前安全清單)
33. [首次起飛流程](#33-首次起飛流程)
34. [速度模式定高操作](#34-速度模式定高操作)
35. [光流方向校正與水平 Trim](#35-光流方向校正與水平-trim)
36. [感測器異常與 Freeze 恢復](#36-感測器異常與-freeze-恢復)
37. [緊急狀況處置](#37-緊急狀況處置)
38. [數據記錄與視覺化](#38-數據記錄與視覺化)

---

# Part 1：ESP32、感測器與藍牙（地面站）

## 1. 所需材料

**地面站與感測器**

| 材料 | 數量 | 備註 |
|------|------|------|
| ESP32 開發板 | 1 | 需具備**藍牙 Classic（SPP）**，例如 ESP32-WROOM-32 DevKit；附 USB 線 |
| VL53L1X 雷射測距模組 | 1 | I2C；VCC 只接 3.3V |
| PMW3901 光流模組 | 1 | SPI；VCC 只接 3.3V；鏡頭朝下對地 |
| 夾爪伺服馬達 | 1 | 一般 SG90/MG90 類，PWM 控制 |
| 杜邦線 | 若干 | ESP32 為標準 2.54mm 排針 |
| PlayStation 手把（藍牙）| 1 | 或用鍵盤替代 |
| Windows 電腦 | 1 | 需內建藍牙或藍牙 USB 接收器 |

**飛控、電調與馬達**

| 材料 | 數量 | 備註 |
|------|------|------|
| 飛控板 F405（Betaflight）| 1 | 需附 Micro USB 線（非 Type-C）|
| 無刷馬達 | 4 | 對角線兩顆 CW、兩顆 CCW |
| 四合一電調（4-in-1 ESC, 55A）| 1 | 一片整合四路；支援 Dshot 或 PWM |
| LiPo 電池 | 1 | 規格依機架與電調而定 |
| **UBEC** | 1 | 直接從電池降壓給 ESP32 供電；建議輸出 5V、額定 ≥2A |
| 電解電容 | 1~2 | 電源輸入濾波 + ESP32 供電緩衝（見第 31 章）|
| 螺旋槳 | 4 | CW / CCW 各兩片，與馬達匹配 |
| 烙鐵、錫線 | — | 焊接電調與電源線用 |

---

## 2. 安裝 Arduino IDE 與 ESP32 開發板支援

### 2-1. 安裝 Arduino IDE

1. 官網下載：https://www.arduino.cc/en/software （選 **Windows Win 10 and newer, 64 bits**）
2. 安裝時**不要取消勾選驅動程式安裝**（會裝 CH340 / CP210x USB 驅動，讓電腦認得 ESP32）

> 多數 ESP32 開發板用 **CP2102** 或 **CH340** USB 晶片。若插上後裝置管理員沒出現 COM 埠，請手動安裝對應驅動。

### 2-2. 加入 ESP32 開發板網址

1. Arduino IDE → **檔案 → 偏好設定（Preferences）**
2. 在「**額外的開發板管理員網址**」貼上：
   ```
   https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json
   ```
3. 按確定

### 2-3. 安裝 ESP32 開發板套件

1. **工具 → 開發板 → 開發板管理員**
2. 搜尋 **`esp32`**
3. 安裝 **「esp32 by Espressif Systems」**

> 此套件已內建 `BluetoothSerial`（藍牙 Classic）、`Wire`、`SPI`，不需另外安裝。

---

## 3. 驗證 ESP32 是否正常（Blink）

先用內建 Blink 範例確認板子通電、能燒錄。

1. USB 連接 ESP32 到電腦
2. **工具 → 開發板** → 選 **ESP32 Dev Module**（或你的板型）
3. **工具 → 連接埠** → 選出現的 COM 埠（拔插板子看哪個埠消失即可確認）
4. **檔案 → 範例 → 01.Basics → Blink**
5. 點上傳

> ⚠️ 部分 ESP32 板燒錄時需在出現「Connecting...」時**按住 BOOT 鍵**直到開始上傳。若上傳卡住，試著按住 BOOT。

✅ 板上 LED 開始閃爍 → 板子正常，繼續下一步。

---

## 4. 安裝感測器與伺服函式庫

韌體 `ino_for_esp32.ino` 需要以下函式庫。**工具 → 管理函式庫（Library Manager）** 逐一搜尋安裝：

| 函式庫 | 搜尋關鍵字 | 用途 |
|--------|-----------|------|
| **VL53L1X by Pololu** | `VL53L1X` | 雷射測距（注意是 **L1X**，不是舊版 L0X）|
| **Bitcraze PMW3901** | `PMW3901` | 光流感測 |
| **ESP32Servo** | `ESP32Servo` | 夾爪伺服馬達（一般 Servo 函式庫在 ESP32 上不可用）|

> 若 Library Manager 找不到 PMW3901，改用手動安裝：到 https://github.com/bitcraze/Bitcraze_PMW3901 下載 ZIP，**草稿碼 → 匯入程式庫 → 加入 .ZIP 程式庫**。

---

## 5. ESP32 接腳總表

以下為韌體 `ino_for_esp32.ino` 寫死的腳位，接線請完全照表。**VL53L1X 與 PMW3901 的 VCC 都只接 3.3V**。

| 模組 | 介面 | ESP32 腳位 |
|------|------|-----------|
| **VL53L1X** | I2C | SDA = **GPIO21**、SCL = **GPIO22**、VCC = **3.3V**、GND = GND |
| **PMW3901** | SPI | MOSI = **GPIO23**、MISO = **GPIO19**、SCK = **GPIO18**、CS = **GPIO5**、NRESET = **GPIO26**、VCC = **3.3V**、GND = GND |
| **夾爪伺服** | PWM | 訊號 = **GPIO13**、VCC = 5V、GND = GND |
| **IBUS（往飛控）** | UART2 | TX = **GPIO17** → 飛控 RX；RX = GPIO16（不使用）|
| **藍牙** | 內建 | 裝置名稱 **`ESP32_Drone_Hub`**（程式內定義，無需接線）|

> 💡 GPIO17（IBUS TX）的接線細節見第 20 章；供電（電池 → UBEC → ESP32）細節見第 31 章。

---

## 6. VL53L1X 測距感測器接線

VL53L1X 是 I2C 雷射測距感測器，提供高度給 ESP32 跑速度 PID 定高。

> ⚠️ VCC **只接 3.3V**，接 5V 會損壞模組。

| VL53L1X | ESP32 |
|---------|-------|
| VCC | **3.3V** |
| GND | GND |
| SDA | **GPIO21** |
| SCL | **GPIO22** |

接好後燒錄韌體（第 9 章），開序列監控應看到 `VL53L1X Init OK`；若顯示 `VL53L1X Init FAILED`，檢查 SDA/SCL 與電源。

---

## 7. PMW3901 光流感測器接線

PMW3901 是 SPI 光流感測器，鏡頭朝下對地，量測水平位移（dx/dy），用於水平漂移記錄與補正。

> ⚠️ VCC **只接 3.3V**。鏡頭要對著**有紋理的地面**，全白/反光/無花紋的地面會量不到（dx/dy 持續為 0）。

| PMW3901 | ESP32 |
|---------|-------|
| VCC | **3.3V** |
| GND | GND |
| MOSI / SDIO | **GPIO23** |
| MISO / SDO | **GPIO19** |
| SCK | **GPIO18** |
| CS / NCS | **GPIO5** |
| NRESET / RST | **GPIO26** |

安裝方向：感測器 **X 軸對機頭右、Y 軸對機頭前**（影響光流補正符號，詳見第 35 章）。

燒錄後序列監控應看到 `PMW3901 Init OK`。韌體內含 watchdog：若連續約 6 秒讀到全零，會自動硬體重置感測器並透過藍牙回報 `OF:REINIT`。

---

## 8. 夾爪伺服馬達接線

夾爪用一顆伺服馬達，由 ESP32 GPIO13 輸出 PWM 控制。

| 伺服馬達 | ESP32 |
|----------|-------|
| 訊號線 | **GPIO13** |
| VCC | 5V |
| GND | GND |

控制邏輯（韌體寫死）：
- **L1（手把）/ Q（鍵盤）** → 轉到 **0°（夾住）**
- **R1（手把）/ E（鍵盤）** → 轉到 **70°（張開）**
- 兩者都不按 → 保持當前角度

上電時伺服會自動歸零到 0°。

---

## 9. 燒錄韌體到 ESP32

1. **燒錄前先拔掉 IBUS（GPIO17）到飛控的線**，避免燒錄時 UART 衝突
2. Arduino IDE → **檔案 → 開啟** → 選 `ino_for_esp32/ino_for_esp32.ino`
3. 確認開發板、COM 埠與第 3 章相同
4. 點上傳，等「上傳完成」（必要時按住 BOOT 鍵）
5. **工具 → 序列埠監控視窗**，鮑率設 **115200**，應看到：
   ```
   ESP32 Bluetooth Started!
   VL53L1X Init OK
   PMW3901 Init OK
   [RF] mm=... valid=1 timeout=0
   ```

> 這個序列監控是日後最重要的偵錯窗口。只要 ESP32 在跑，它就會持續印 `[RF] mm=...`。看不到 = ESP32 沒在跑或一直重啟（見第 31 章）。

---

## 10. 電腦配對 ESP32 藍牙

ESP32 用**內建藍牙 Classic（SPP）**，不需要 HC-05 模組。

### 10-1. 配對

1. 開始選單 → 設定 → **藍牙與裝置** → 新增裝置 → 藍牙
2. 選擇 **`ESP32_Drone_Hub`**（若需密碼試 **1234** / **0000**）
3. 顯示已配對即可

### 10-2. 找出「傳出（Outgoing）」COM 埠

1. 開始選單 → 搜尋「**裝置管理員**」→ 展開「**連接埠（COM 和 LPT）**」
2. 會看到兩個藍牙串列埠：一個**傳入（Incoming）**、一個**傳出（Outgoing）**
3. **記下「傳出」那個 COM 埠號** —— 這才是 Python 要連的

> ⚠️ **最常見的坑**：連到「傳入」埠會出現「埠開得起來、`write` 也成功、但完全收不到回傳」的假連線。Python 端 `send_packet` 只看寫入有無例外，無法分辨真假連線，所以**務必連傳出埠**。

---

## 11. 安裝 Python 環境

建議使用 python.org 的 **Python 3.11 x64**（不要只用 Microsoft Store 版本）。

```bash
py -3.11 -m pip install --upgrade pip setuptools wheel
py -3.11 -m pip install pyserial pygame keyboard matplotlib
```

| 套件 | 用途 |
|------|------|
| `pyserial` | 藍牙序列通訊 |
| `pygame` | 手把輸入 + 狀態視窗 |
| `keyboard` | 鍵盤模式全域輸入 |
| `matplotlib` | 主程式右側即時光流/高度/馬力圖表 |

> 若 `pygame` 安裝出現 `Failed to build wheel`，通常是 Python/pip 安裝不完整或誤用 Store 版 Python。先確認 `py -3.11 -m pip --version` 正常，否則改裝 python.org 版本。

---

## 12. 用 test_esp32_bt.py 測試藍牙通訊

`tests/test_esp32_bt.py` 是專門的藍牙診斷工具，會：
1. 經 USB 重置 ESP32 並確認它在跑韌體
2. 經藍牙送封包，等 ESP32 回傳遙測
3. 印出診斷結論（區分「假連線」「埠填錯」「ESP32 沒跑」等情況）

開檔修改最上方兩個埠：

```python
BT_PORT  = 'COM13'   # ← ESP32 藍牙「傳出」COM 埠（第 10-2 查到的）
USB_PORT = 'COM10'   # ← ESP32 USB COM 埠（可設 None 跳過 USB 診斷）
```

執行：

```bash
python tests/test_esp32_bt.py
```

✅ 看到 `ESP32 有收到藍牙封包，並回傳遙測資料` → 藍牙鏈路正常。
❌ 完全無回傳 → 依程式印出的結論排查（最常見是連到「傳入」埠，或埠號填錯）。

---

## 13. 測試手把連接

### 13-1. 配對手把

設定 → 藍牙與裝置 → 新增裝置 → 選手把（PS4 手把同時按住 **Share + PS** 進入配對）。

### 13-2. 確認按鍵編號

```bash
python tests/test_joy.py
```

按下每個鍵，確認編號與 `main_esp32_velmode.py` 上方定義一致：

```python
BTN_CIRCLE = 1     # ○ 定高切換
BTN_CROSS  = 0
BTN_SQUARE = 2     # □ 搖桿校準
BTN_TRI    = 3     # △ 解鎖/上鎖
BTN_L1     = 9     # 夾爪 夾
BTN_R1     = 10    # 夾爪 開
BTN_UP, BTN_DOWN, BTN_LEFT, BTN_RIGHT = 11, 12, 13, 14
BTN_OPTIONS = 6    # 長按 3 秒退出
BTN_ESTOP_A = 4    # 4+6 緊急停機
BTN_ESTOP_B = 6
BTN_CALIB   = 15   # 右搖桿偏移補正
```

換不同廠牌手把若編號不同，直接改這裡的數字即可。

---

## 14. 執行主程式

用 `--port` 指定 ESP32 藍牙「傳出」COM 埠（預設 COM9）：

```bash
python main_esp32_velmode.py --port COM9
```

程式啟動會**自動偵測手把**：有手把 → 手把模式；沒有 → 鍵盤模式。
啟動時若藍牙未連線，會每 3 秒重試、最多等 60 秒。連上後終端機顯示：

```
✅ 成功連線到 COM9
```

並出現一個視窗：左側狀態面板（連線/解鎖/高度/馬力/通道），右側即時圖表（光流 dx/dy、量級、高度、累積路徑、FC 平均馬力）。

> ⚠️ 尚未接飛控時，操作不會有飛行動作，僅確認數值輸出與遙測接收正常即可。
>
> 此時序列監控能不能看到 `D:`/`OF:` 持續回傳，是判斷整條鏈路是否健康的最快方法。

---

## 15. （備用）鍵盤控制模式

沒有手把時，`main_esp32_velmode.py` 會自動切換為鍵盤模式（全域輸入，不需點選視窗）。

| 按鍵 | 手動模式 | 速度定高模式（H 開啟後）|
|------|----------|----------------------|
| `W` / `S` | 油門增減 | 爬升 / 下降速率 |
| `A` / `D` | 偏航 | 偏航 |
| `↑` / `↓` | 俯仰 | 俯仰 |
| `←` / `→` | 翻滾 | 翻滾 |
| `Tab` / `Shift` | 基準油門 上/下 | （無效）|
| `C` / `Z` | 步進量 增大/縮小 | — |
| `Q` / `E` | 夾爪 夾合 / 張開 | 夾爪 夾合 / 張開 |
| `R` | 解鎖 / 上鎖 | 解鎖 / 上鎖 |
| `H` | 切換定高（開啟）| 切換定高（關閉）|
| `X` 長按 3 秒 | 安全退出 | 安全退出 |

手把模式對照：○=定高切換、△=解鎖/上鎖、□=搖桿校準、按鍵 15=右搖桿偏移補正、L1/R1=夾爪、D-pad 上下=基準油門、Options 長按 3 秒=退出、**4+6=緊急停機**。

---

# Part 2：ESP32 與 Betaflight 飛控連接

## 16. 連接前注意事項

> ⚠️ ESP32 為標準 2.54mm 排針，但飛控板（F405）多為焊盤或小孔，**無法用一般杜邦線穩定插接**。

暫時固定方式：
- **膠帶固定法**：杜邦線母頭壓在焊盤上用膠帶固定
- **穿孔法**：剪出母頭裸銅穿過焊盤小孔彎折固定

長期使用建議直接焊接（待 Part 3 全部測通後再焊，見第 20-3）。

---

## 17. 飛控板連接電腦與 Betaflight 韌體

### 17-1. 用 USB 連接飛控板

> ⚠️ **必須使用 Micro USB**，不可用 Type-C。部分 F405 的 Type-C 僅供電不支援資料傳輸，電腦會偵測不到。

### 17-2. 安裝 Betaflight Configurator

GitHub 下載最新版：https://github.com/betaflight/betaflight-configurator/releases
下載 `betaflight-configurator-installer_x.x.x_win64.exe` 並安裝。

### 17-3. 連線

1. 開啟 Betaflight Configurator
2. 右上角選飛控板的 COM 埠，鮑率保持預設（115200）
3. 點 **Connect**，左側出現選單代表成功

### 17-4. 更新韌體（如需要）

1. 左側 **Firmware Flasher**
2. 確認板型（例如 `STM32F405`）
3. **Load Firmware [Online]** → **Flash Firmware**
4. 燒錄中飛控會自動重啟，不要拔 USB

---

## 18. 設定 IBUS 通訊協議

ESP32 透過 IBUS 把 RC 訊號送給 Betaflight。

### 18-1. 串列埠（Ports）

1. 左側 **Ports**
2. 找到接 ESP32 IBUS 的 UART（ESP32 GPIO17 接到飛控哪個 UART RX）
3. 將該 UART 的 **Serial RX** 開啟
4. **Save and Reboot**

### 18-2. 接收機模式（Configuration / Receiver）

1. 左側 **Configuration** → **Receiver** 區塊
2. **Receiver Mode** → `Serial-based receiver`
3. **Serial Receiver Provider** → `IBUS`
4. **Save and Reboot**

---

## 19. 陀螺儀校正

1. 飛控板放在**水平靜止**平面
2. 左側 **Setup** → **Calibrate Accelerometer**，等約 5 秒
3. 3D 模型回到水平姿態即完成

> 每次更換安裝位置或重新固定飛控後，建議重新校正。

---

## 20. ESP32 與飛控板接線

### 20-1. 訊號接線（測試階段）

測試階段兩塊板可各自用 USB 供電，只需兩條訊號線：

| 從哪裡 | 到哪裡 | 說明 |
|--------|--------|------|
| ESP32 **GPIO17（TX）** | 飛控 **UART RX**（第 18-1 設定的那個）| IBUS 訊號 |
| ESP32 **GND** | 飛控 **GND** | 共地（必接，否則訊號不穩）|

> ⚠️ IBUS 從 ESP32 的 **GPIO17** 送出（UART2 TX）。不要接錯到其他腳。

### 20-2. 確認接線

兩板各自 USB 接電腦：ESP32 在 Arduino IDE 看得到 COM 埠、飛控在 Betaflight 能 Connect，即可進第 22 章確認訊號。

### 20-3. 焊接永久連線

> ⏳ 建議等 Part 3 全部完成（馬達轉向確認、整合測試通過）後再焊接，避免焊完才發現問題。

正式供電接線（含UBEC）詳見**第 31 章**。訊號線建議用 **24 AWG**，焊點確實不虛焊。

---

## 21. Betaflight 安全設定

### 21-1. 關閉 Airmode

Airmode 會在解鎖時對馬達持續輸出最低轉速，地面帶槳測試可能爆衝翻車。

1. **Configuration → Other Features**
2. 確認 **`AIRMODE`** 為**關閉**（灰色）
3. **Save and Reboot**

### 21-2. Failsafe（建議）

藍牙斷線時飛控會持續收到最後一筆 IBUS。建議設定降落型 Failsafe：

- **Failsafe → Stage 2 → Drop 或 Land**

---

## 22. 通道對應與訊號確認

### 22-1. 設定通道對應（Channel Map）

ESP32 送出的通道順序為 Roll/Pitch/Throttle/Yaw（見下表），需把 Betaflight 的 **Channel Map** 設成 **`AETR1234`**。

1. 左側 **Receiver** → **Channel Map** → `AETR1234`
2. **Save and Reboot**

ESP32 → 飛控的 IBUS 通道對應（韌體寫死）：

| 通道 | 功能 | 範圍 |
|------|------|------|
| CH1 | 翻滾 Roll | 1000~2000 |
| CH2 | 俯仰 Pitch | 1000~2000 |
| CH3 | 油門 Throttle（手動）/ PID 輸出（定高）| 1000~2000 |
| CH4 | 偏航 Yaw | 1000~2000 |
| CH5 | 解鎖 Arm | 1000 / 2000 |
| CH6 | 飛行模式 ANGLE（固定 2000）| 2000 |
| CH7 / CH8 | 固定 1000（不使用飛控定高/定點）| 1000 |

> 定高完全由 ESP32 用 VL53L1X 自行運算後調整 CH3 油門達成，**不使用 Betaflight 的 ALTHOLD/POSHOLD**，所以 CH7/CH8 固定 1000。

### 22-2. 確認訊號

1. 執行 `python main_esp32_velmode.py`
2. 回 Betaflight **Receiver** 分頁
3. 不動搖桿時 **Throttle 應穩定在 1000**
4. 移動各搖桿，對應通道有正確變化

✅ 數值正確變化 → IBUS 通訊正常。
❌ 全部靜止 → 回查第 18 章 UART 設定與第 20 章接線。

---

## 23. 模式綁定

進入 **Modes** 分頁綁定通道。

### 23-1. ARM（解鎖）

程式預設 arm=1000（上鎖），按 △ 後送 2000（解鎖）。

1. **ARM** 那列 → **Add Range**
2. 通道選 **AUX 1**
3. 滑桿範圍設 **1800~2100**（只涵蓋 2000，不涵蓋 1000）
4. **Save**

### 23-2. ANGLE（自穩模式）

韌體把 CH6（AUX 2）固定輸出 2000，代表自穩永遠啟動。

1. **ANGLE** 那列 → **Add Range**
2. 通道選 **AUX 2**
3. 滑桿範圍 **1800~2100**
4. **Save**

---

## 24. 解鎖失敗排查

按 △（或 R）無法解鎖時，進 **Setup** 看右側 **Arming Disable Flags**：

| Flag | 原因 | 解決 |
|------|------|------|
| `MSP` | 飛控仍連著 Betaflight | 拔 USB、斷開 Betaflight 連線 |
| `RX_FAILSAFE` | 訊號中斷 | 確認 ESP32 在送 IBUS；重新上電讓兩板同步啟動 |
| `THROTTLE` | 油門未歸零 | 按 □ 重新校準搖桿，確認油門送 1000 |
| `ARM_SWITCH` | 解鎖開關未復位 | 確認程式 `arm_state` 為 0（上鎖）再解鎖 |

---

# Part 3：電調、馬達與電源

## 25. 焊接安全警告

> 🔥 **焊接危險，操作前確保：**
> - **通風良好**（焊錫煙霧有害）
> - 烙鐵 300°C 以上，**勿觸碰烙鐵頭**，用完立刻放回架上
> - 工作台周圍**無易燃物**
> - 建議戴護目鏡防噴濺
> - 焊完確認烙鐵**完全冷卻**再收納

---

## 26. 電調與電源線焊接

### 26-1. 確認電源線極性

| 線色 | 極性 |
|------|------|
| 紅色 | 正極（+）|
| 黑色 | 負極（−）|

用三用電表「直流電壓」檔：紅棒接電池正、黑棒接負，確認讀值為正再對應焊接。接反會立即燒毀電調與飛控。

### 26-2. 確認電容極性

- 電容本體**白/灰條紋側為負極（−）**，較長引腳為正極（+）
- 正極接電源正極焊盤，負極接地

> ⚠️ 電解電容接反通電會膨脹甚至爆炸，務必確認方向。

### 26-3. 焊接順序

1. 先把電容焊到電調電源輸入焊盤（靠近 XT60 處）
2. 再焊電源線（XT60）到電調電源輸入焊盤
3. 確認每個焊點光滑飽滿，無虛焊/冷焊

---

## 27. 電調與馬達焊接

### 27-1. 原理

無刷馬達有 3 條相線，電調有 3 個輸出焊盤：
- 依序對接 → 某方向旋轉
- **任意兩條對調 → 反轉**

### 27-2. 步驟

1. 馬達三線剝皮約 3mm、鍍錫
2. 電調三個輸出焊盤先點錫
3. 依序焊上，輕拉確認不脫落
4. 四顆馬達重複

### 27-3. 對應關係

| 電調輸出 | 馬達位置 | Betaflight 編號 |
|----------|----------|-----------------|
| M1 | 右前 | Motor 1 |
| M2 | 左後 | Motor 2 |
| M3 | 左前 | Motor 3 |
| M4 | 右後 | Motor 4 |

> 實際以 Betaflight Setup 頁面圖示為準。

---

## 28. 上電安全檢查

### 28-1. 接電池前

- 目視所有焊點，無短路、無金屬碎屑
- 再次確認電源正負極
- 馬達線無裸銅互觸

### 28-2. 第一次接電池

> ⚠️ 第一次通電時，雙眼專注盯著飛控與電調，手放在電池接頭旁，異常立刻斷電。

正常現象：飛控 LED 亮、電調「嗶嗶」啟動音、**無冒煙/異味/過熱**。

若冒煙或異味：**立刻拔電池** → 待冷卻 → 目視找燒焦/變色元件 → 重焊問題焊點再測。

---

## 29. 用 Betaflight 確認馬達連接

> ⚠️ **測試前拆除所有槳葉。**
> 💡 未焊完四顆可先測一顆。
> 📌 此步驟**不需要接 ESP32**，只需飛控 USB + 電池供電。

### 29-1. 進入馬達測試

1. 飛控 USB 接電腦（電池同時供電）
2. Betaflight → **Motors**
3. 勾選安全確認框（「I understand the risks...」）

### 29-2. 逐一測試

1. **Master** 維持 0
2. 拖 **Motor 1** 到約 1100，看對應馬達是否轉
3. 確認後歸零，依序測 2/3/4

### 29-3. 確認轉向

```
      前
  CW  ↑  CCW
  (2)   (1)
    ╲   ╱
    ╱   ╲
  (3)   (4)
  CCW      CW
```

> 對角線同向、相鄰反向。

### 29-4. 反轉方向（不需重焊）

1. 確保已接電池（ESC 需供電才能設定）
2. Betaflight → **Motors** → 每顆馬達的 **Motor Direction** 下拉（`Normal` / `Reversed`）
3. 改錯誤者為 `Reversed` → **Save** → 重測

> 舊版 Betaflight 若無此選項，改用 BLHeli Configurator，或直接對調該馬達任意兩條相線重焊。

---

## 30. 手把控制馬達整合測試

> ⚠️ **測試前拆除所有槳葉。**
> ⚠️ 再次確認：Betaflight **AIRMODE 已關閉**（第 21 章）、**Motors 頁安全框已勾**。

### 30-1. 連接硬體

1. ESP32 USB 接電腦
2. 飛控 USB 接電腦
3. 接電池（供電給電調馬達）
4. 確認 ESP32 GPIO17 → 飛控 RX，GND 共地

### 30-2. 開 Betaflight Motors 頁並勾安全框

### 30-3. 執行控制程式

```bash
python main_esp32_velmode.py --port COM9
```

### 30-4. 測試流程

1. 確認油門歸零
2. 按 **△**（或 `R`）解鎖
3. 緩慢推油門，看 Motors 頁數值與馬達是否隨之變化

✅ 馬達隨輸入轉動 → 整合測試通過。
❌ 無反應 → 依序查：Receiver 訊號（第 22-2）→ ARM 是否解鎖（第 24）→ Motors 安全框是否勾。

---

## 31. ⭐ ESP32 供電與 Brownout 斷線排查

> 這一章記錄本專題實測踩過的關鍵問題，**裝槳上電飛行前務必先看**。

### 31-1. 供電架構

ESP32 直接從電池取電，經 **UBEC** 降到 5V 再供 ESP32：

```
電池 ──┬── UBEC(5V) ── ESP32
       └──（同一顆電池）── 四合一電調(55A) ── 四顆馬達
```

ESP32 的 UBEC 與電調共用同一顆電池，這是 brownout 問題的根源。

### 31-2. 典型症狀：「馬力催高就斷線、拆槳就正常」

實測現象與**單一根因**：

```
裝槳 + 馬力催高（例：油門催到 ~90）
   → 馬達電流暴增（可達數十安培）
   → 電池端電壓瞬間驟降
   → UBEC 輸入被拖垮、輸出 5V 跟著掉
   → ESP32 觸發 brownout 重啟
   → 藍牙斷線（但電腦端 COM 埠還開著）
   → 收不到 D:/OF: 遙測、飛控也收不到 IBUS
```

**拆掉螺旋槳 = 馬達空轉、電流極小 = 電壓不掉 = ESP32 不重啟 = 一切正常。**
所以「拆槳就好」不是巧合，是 brownout 的決定性證據。

### 31-3. 容易誤判的假象

- **「串口可以寫入但傳不到飛控、也收不到感測器」**：藍牙虛擬 COM 埠在 ESP32 已重啟/斷線時，`write()` 仍會「成功」（資料只進 Windows 緩衝區）。**寫入成功 ≠ ESP32 收到**。真正的判斷依據是「有沒有收到 `D:`/`OF:` 回傳」。
- **「剛剛還可以、突然壞掉」**：運行中 brownout 重啟或藍牙掉線的典型表現，不是接線/設定錯誤。

### 31-4. 確認是不是 brownout

USB 接 ESP32、開序列監控（115200）讓它連著跑，**在又斷線的當下**看監控：

| 監控畫面 | 判定 |
|----------|------|
| 跳出開機訊息（`ESP32 Bluetooth Started!` 再印一次）或 `Brownout detector was triggered` | **確定是重啟 → 供電問題** |
| 仍正常印 `[RF] mm=...` | ESP32 沒掛，是**純藍牙掉線** |

同時用萬用表量「馬力催高當下」UBEC的**輸入**與**輸出**電壓：
- 輸入掉很多 → 電池 / 取電線徑 / 取電點問題
- 輸入穩但輸出掉 → UBEC力不足或需加電容

### 31-5. 解法（依重要性）

1. **UBEC 輸出端並大電容**（470~1000µF 電解，注意極性）—— 最便宜有效，先做。
2. **ESP32 供電盡量與馬達電流路徑分開**：從電池端 **就近單獨拉粗線**給 UBEC，焊點牢固；或乾脆給 ESP32 獨立電池。
3. **電源線加粗、接點檢查**：XT60、香蕉頭、電池焊點都要牢，細線/鬆動在大電流下壓降嚴重。
4. **UBEC 電流要夠**：ESP32 + 藍牙瞬間可衝到 ~500mA，建議額定 ≥2A，輸入電壓要比輸出高 1~2V 餘裕。
5. **電池要夠力**：內阻大/老化/C 數不足都會加重壓降，可換好電池對照。

### 31-6. 驗證修好

加電容/改線後，**裝槳**催到原本會斷的馬力，三項同時成立才算修好：
- 序列監控不再跳重啟 ✅
- Python 持續收到 `D:`/`OF:` ✅
- Betaflight Receiver 通道持續有訊號 ✅

---

# Part 4：實機飛行與速度模式定高

> ⚠️ 進入此部分前，確認 Part 1~3 全部通過：藍牙鏈路正常（收得到遙測）、IBUS 訊號正常、馬達轉向正確、**供電 brownout 已排除（第 31 章）**。

---

## 32. 飛行前安全清單

| 項目 | 確認方式 |
|------|----------|
| 槳葉方向正確 | CW/CCW 與馬達轉向匹配 |
| 槳葉螺絲鎖緊 | 手轉無鬆動 |
| 電池電量充足 | 電壓計/充電器確認 |
| 供電穩定（不 brownout）| 已完成第 31 章驗證 |
| 高度數值正常 | 主程式狀態面板顯示 cm 數值（非 `---`）|
| 光流有資料 | 右側圖表 dx/dy 隨移動有反應 |
| 飛行環境淨空 | 周圍 3 公尺內無人員與障礙 |
| 緊急停機可及 | 手把確認 **4+6** 按得到；鍵盤確認 **X** 可及 |
| Betaflight 已斷線 | 飛控不接 USB（避免 MSP flag 卡解鎖）|

> ⚠️ 飛行測試至少兩人：一人操控、一人在旁準備斷電。

---

## 33. 首次起飛流程

### 33-1. 啟動

1. 接電池給整機供電（ESP32 經 UBEC 上電）
2. 等 ESP32 藍牙與電腦連上（Windows 顯示 `ESP32_Drone_Hub` 已連線）
3. 手把開機並配對
4. 執行：
   ```bash
   python main_esp32_velmode.py --port COM9
   ```
5. 等 `✅ 成功連線到 COMx`，確認狀態面板高度數值正常

### 33-2. 手動模式試飛

首次飛行請用**手動模式**：

1. 搖桿置中，確認油門輸出 0
2. 按 **△**（或 `R`）解鎖（程式會先送 0 油門 0.25 秒再真正解鎖）
3. 慢慢推油門（左搖桿上 / `W` / D-pad 上）至起飛
4. 保持低空（15~30 cm），確認姿態穩定
5. 油門歸零慢慢降落
6. 落地按 **△** 上鎖

> 起飛後若姿態異常（翻滾/偏轉），立即**緊急停機**（4+6 / X 長按），降落後重查馬達轉向與槳葉方向。

---

## 34. 速度模式定高操作

定高由 **ESP32 端速度 PID** 完成：你給的是「上升/下降速率」，ESP32 自動調 CH3 油門維持高度。

### 34-1. 切入

1. 手動飛到穩定懸停高度（建議 15~30 cm）
2. 按 **○**（手把）或 **H**（鍵盤）切入
3. 狀態面板顯示「定點定高中」與 FC 平均馬力

> 空中切入會檢查油門是否置中（偏移過大會擋下，避免暴衝）；地面也可待機切入。

### 34-2. 定高中的搖桿

| 左搖桿（油門軸）| 效果 |
|-----------------|------|
| 死區內（置中）| 速度命令 0，維持當前高度 |
| 推上 | 正速度命令，爬升（最大 60 cm/s）|
| 推下 | 負速度命令，下降（最大 60 cm/s）|

定高中其他鍵：**○/H** 退出、**△/R** 上鎖（同時退出）、**L1/R1（Q/E）** 夾爪、**按鍵 15** 水平 trim（見第 35 章）。

### 34-3. 退出

1. 按 **○**（或 `H`）退出
2. ESP32 回傳當前油門（`T:`），主程式自動同步基準油門
3. 顯示 `✅ 基準油門已同步：xxx` 後可接手手動油門

> 同步逾時（約 1.5 秒沒收到 `T:`）會自動解除凍結，以舊基準續控。

### 34-4. 收斂時間

| 狀況 | 預期 |
|------|------|
| 切入前手動油門合理（IBUS ≥ 1250）| 1~2 秒穩定懸停 |
| 從地面直接啟動 | 3~5 秒緩升後穩定 |
| 懸停後推桿爬升再放開 | 積分已收斂，快速回穩 |

---

## 35. 光流方向校正與水平 Trim

### 35-1. 目前狀態

PMW3901 光流會持續記錄並在右側圖表即時顯示（dx/dy、量級、累積路徑）。**自動光流補償（P+I）目前在程式中停用**（已註解），實際生效的是**手動右搖桿 trim（按鍵 15）**。

### 35-2. 用 test_flow_direction.py 校正方向

啟用自動補償前，必須先確認光流補正的「軸向」與「符號」正確，否則會正回饋越飄越開。工具會引導你地面平移機體、自動判讀：

```bash
python tests/test_flow_direction.py --port COM9
```

> 本工具只「讀」遙測、不送控制封包；仍**強烈建議拆槳**、對著有紋理地面、抓常用飛行高度（30~60cm）。

它會告訴你 `main_esp32_velmode.py` 的 `FLOW_ROLL_SIGN` / `FLOW_PITCH_SIGN` 該維持還是改號。

### 35-3. 按鍵 15：飛行中水平 Trim

按下 **按鍵 15** 時，程式讀取當下右搖桿 pitch/roll 偏移，累加到內部 trim 並暫時凍結右搖桿；**把右搖桿回中後**這次補正才生效。

正確用法（避免過度校正）：
1. 起飛後先進入穩定懸停
2. 用右搖桿把飄移修到**很小**
3. 在「現在幾乎不飄」的瞬間按 **15**
4. **立刻把右搖桿回中**
5. 等終端機出現 `[Trim] 搖桿已回中，補正生效` 再續操作
6. 仍有輕微飄移就再做一次**小量**補正，不要一次補太多

> 過度校正的典型現象：原本小飄移消失後，機體改成持續往反方向慢慢飄。

---

## 36. 感測器異常與 Freeze 恢復

### 36-1. 高度 Freeze（VL53L1X）

VL53L1X 若連續 5 次讀值與前次差距 > 200mm（飛上平台、被遮擋等），判定高度真實大幅改變，觸發保護：

1. `filtered_mm` 直接換成最新原始讀值（跳過 EMA）
2. 清除 PID 狀態（積分、速度估計、prev 值）
3. ESP32 送 `F:1` 通知 Python，**並自動退出定高**（避免舊積分暴衝）；恢復正常時送 `F:0`

### 36-2. 光流 watchdog（PMW3901）

連續約 6 秒讀到全零 → 判定感測器卡死 → 自動硬體重置並重初始化，藍牙回報 `OF:REINIT` / `OF:REINIT_OK` / `OF:REINIT_FAIL`。

### 36-3. 排查表

| 現象 | 可能原因 | 處置 |
|------|----------|------|
| 高度顯示 `---` | 感測器未回傳 | 確認 VCC=3.3V、SDA(21)/SCL(22) 接線；序列監控看 `VL53L1X Init` |
| 高度跳動劇烈 | 反射面不平或姿態傾斜大 | 確認姿態；調低韌體 `ALPHA`（加強 EMA）|
| 高度凍結不動 | 藍牙掉包或 timeout | 確認藍牙連線；重啟 ESP32 |
| 光流 dx/dy 一直 0 | 地面無紋理/太暗/太低 | 換有紋理地面、拉高、確認 `PMW3901 Init OK` |
| 收得到能寫、卻沒遙測 | brownout/藍牙假連線 | 見第 31 章；確認連的是「傳出」COM 埠 |

---

## 37. 緊急狀況處置

### 37-1. 緊急停機

任何時候失控立即執行：
- **手把**：同時按住 **按鍵 4 + 按鍵 6**
- **鍵盤**：長按 **X 鍵 3 秒** 安全退出，或直接斷電

所有緊急路徑（緊急停機、手把斷線、正常退出、串口寫入失敗）共用同一上鎖流程：
1. Python 立即 `arm_state=0`、`alt_hold_active=False`、停止記錄
2. 送 **5 次**全停封包（油門 0、arm=0、定高關）
3. 程式結束或等重連

ESP32 收到 arm=0 後，CH5 降至 1000，飛控上鎖、馬達停轉。

### 37-2. 藍牙斷線

主程式背景每 3 秒自動重連，重連成功後 `arm_state` 自動歸 0（需重新解鎖）。串口寫入失敗（`send_packet` 回 False）也會立即上鎖並重連。

> ⚠️ 但藍牙「假連線」（埠開著但沒真連）抓不到，所以請務必連「傳出」埠，並以「有無遙測」判斷真實連線。配合第 21-2 的飛控 Failsafe 作為最後保險。

### 37-3. 定高下的異常

| 現象 | 立即處置 |
|------|----------|
| 切入後高度持續上升/下降 | 立刻按 **○**（H）切回手動接管 |
| 油門劇烈振盪 | 立刻切手動；調低韌體 `Kp_vel` 後重試 |
| 高度暴衝超出預期 | 立刻 **緊急停機**（4+6 / X）|

---

## 38. 數據記錄與視覺化

### 38-1. PID 數據（vel_logs/）

解鎖且開啟定高時自動記錄至 `vel_logs/velmode_YYYYMMDD_HHMMSS.csv`，可用以下繪圖：

```bash
python plot_pid.py
```

### 38-2. 光流數據（flow_logs/）

解鎖後自動記錄光流 dx/dy 與高度至 `flow_logs/flow_*.csv`，繪圖：

```bash
python plot_flow.py
```

### 38-3. 通訊協議（開發參考）

**電腦 → ESP32（藍牙，10 bytes）**
```
[ 'S' | Throttle | Yaw | Pitch | Roll | AltVel | Gripper | Arm | AltHold | XOR ]
```
各值 0~255。`AltVel`：128=0 cm/s，每 1 = 1 cm/s。`AltHold`：1=定高、0=手動。`XOR`=前 8 bytes 的 XOR。

**ESP32 → 電腦（藍牙遙測）**

| 格式 | 說明 | 時機 |
|------|------|------|
| `D:xxxx\n` | 高度（mm）| 每 200ms |
| `OF:dx,dy\n` | 光流增量 | 每 40ms（25Hz）|
| `OF:REINIT[_OK/_FAIL]\n` | 光流 watchdog | 觸發時 |
| `THR:xxxx\n` | 定高中 ESP32 PID 油門（IBUS）| 每 200ms |
| `T:xxxx\n` | 退出定高時最後油門 | 退出時一次 |
| `F:1` / `F:0` | 高度 Freeze 進入/恢復 | 觸發時 |

**ESP32 → 飛控（IBUS，115200，每 20ms）**：32-byte 標準 IBUS，通道對應見第 22-1。
